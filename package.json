{
  "name": "arcade",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "gh-pages": "tsc && vite build --base='/phaser-tutorial/'"
  },
  "devDependencies": {
    "typescript": "^4.5.4",
    "vite": "^2.8.0"
  },
  "dependencies": {
    "phaser": "^3.55.2"
  }
}
